# if you need a github token:
# from fake_token import token

import os
os.system("pwd")

# do stuff inside the repo.
# i.e. edit files, create files, remove files, use gh CLI to examine or interact with issues/PRs,
#  use git CLI to manipulate the repository


# slow down to avoid github rate limit
time.sleep(3)